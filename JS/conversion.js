let value = "44";

console.log(typeof value);

let valueInNumber = Number(value);

console.log(typeof valueInNumber);      //conversion into number

console.log(valueInNumber); //checking the output of conversion 


// so in the conversion we know to what things are convert into which form

// '44' = this is a string and we want to convert it into number and it convert but the data type is string 
// 33output = this kind string and number is nan not a number declear in js 
// true = is 1 and false is 0 in js

// so in the conversion we know to what things are convert into which form

let isLoggedIn = ""

let booleanIsLoggedIn = Boolean(isLoggedIn)

console.log(booleanIsLoggedIn);

// in the conversion of boolean 
// if the filed is have value it conversion into true 
// if the field is empty value it conversion into false

let MeroPaisa =  400

let stringBoolean = Boolean(MeroPaisa);

console.log(stringBoolean);

console.log(typeof(stringBoolean))

